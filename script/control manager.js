/*
    Manages the control inputs
*/

function setKey() {
    var key = event.key;
    // set the key as true in the control array

}

function unsetKey() {
    var key = event.key;
    // set the key as false in the control array

}